<% provide(:title, "Pagina's") %>

<div class="action_bar_right mt-10">
  <%= link_to '<i class="fa fa-plus"></i>'.html_safe, new_admin_content_page_path, class: "btn-u btn-u-default", title: "Pagina toevoegen" %>
</div>

<h1><%= yield(:title) %></h1>

<table class="table">
  <thead>
    <tr>
      <th></th>
      <th>URL</th>
      <th>Titel</th>
      <th>Inleiding</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <%= render @content_pages %>
  </tbody>
</table>
